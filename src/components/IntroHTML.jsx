import React from 'react';

function IntroHTML() {
  return (
    <section>
      <h2>Introducción a HTML</h2>
      <p>HTML (HyperText Markup Language) es el lenguaje estándar para crear páginas web. Define la estructura de una web a través de etiquetas que organizan el contenido.</p>
    </section>
  );
}

export default IntroHTML;
