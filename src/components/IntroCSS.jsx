import React from 'react';

function IntroCSS() {
  return (
    <section>
      <h2>Introducción a CSS</h2>
      <p>CSS (Cascading Style Sheets) se usa para definir el estilo visual de una página web. Con CSS puedes controlar el color, fuente, márgenes y la disposición de los elementos en la pantalla.</p>
    </section>
  );
}

export default IntroCSS;
